var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  res.render('index', { title: 'Express version 3.0', text:'Express rendering' });
});

module.exports = router;
